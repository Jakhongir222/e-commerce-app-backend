debug: true
server:
  port:
spring:
  sql:
    init:
      platform: postgresql
      mode: always
  output:
    ansi:
      enabled: always
  datasource:
    driver-class-name:
    url: jdbc:postgresql://snuffleupagus.db.elephantsql.com/uknrgsln
    username: uknrgsln
    password: knrWZW_cpHR0cjMN5KHoohmnB9Mf-khc

  jpa:
    show-sql: true
    generate-ddl: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none
logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure: ERROR